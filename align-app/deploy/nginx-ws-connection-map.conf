# Global (http context). Install ONLY as:
#   /etc/nginx/conf.d/ws-connection-map.conf
# Loaded via: include /etc/nginx/conf.d/*.conf;
#
# Do NOT duplicate this map in any server{} or sites-* file.

map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
